/**
 * Top-level build file for ftc_app project.
 *
 * It is extraordinarily rare that you will ever need to edit this file.
 */

buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        // Note for FTC Teams: Do not modify this yourself.
        classpath 'com.android.tools.build:gradle:8.7.0'
    }
}

// This is now required because aapt2 has to be downloaded from the
// google() repository beginning with version 3.2 of the Android Gradle Plugin
allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

repositories {
    mavenCentral()
}

def danceBotCommand = { List args = [] ->
    def isWindows = System.getProperty('os.name').toLowerCase().contains('windows')
    def launcher = isWindows ? ['cmd', '/c', 'launch-dancebot.bat'] : ['./launch-dancebot']
    launcher + args
}

tasks.register('launchDanceBot', Exec) {
    group = 'ftc'
    description = 'Launches DanceBot Auto over Robocol UDP. Connect to Control Hub Wi-Fi first.'
    workingDir rootDir
    commandLine danceBotCommand()
}

tasks.register('launchDanceBotMotorTest', Exec) {
    group = 'ftc'
    description = 'Launches the one-motor-at-a-time DanceBot diagnostic. Connect to Control Hub Wi-Fi first.'
    workingDir rootDir
    commandLine danceBotCommand(['motor-test'])
}

tasks.register('listDanceBotOpModes', Exec) {
    group = 'ftc'
    description = 'Lists OpModes reported by the Robot Controller. Connect to Control Hub Wi-Fi first.'
    workingDir rootDir
    commandLine danceBotCommand(['list'])
}
